/**
 * Entry point for the rskj-regression driver.
 *
 * This is a placeholder for the scaffolding task. The driver and orchestrator
 * implementations land in subsequent tasks of the regression-testing
 * initiative; see README.md for the roadmap.
 */

export const VERSION = "0.0.0";

export function describe(): string {
  return `rskj-regression v${VERSION} (scaffolding only)`;
}
